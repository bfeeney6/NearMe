version: '3'
services:

  database:
    build: ./nearmedatabase
    restart: always
    container_name: nearmedatabase
    environment:
      MYSQL_DATABASE : 'nearme'
      MYSQL_ROOT_PASSWORD: 'root'
    ports: 
      - '3306:3306'
